<nav class="header navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-navbar" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="fa fa-ellipsis-v"></span>
            </button>

            {% include 'MusicToolsMusicianBundle:Musician:menu.html.twig' %}
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="header-navbar">
            {# Guitar menu #}
            <guitar-menu class="nav navbar-nav"></guitar-menu>

            <ul class="nav navbar-nav">
                <li class="nav-link-default" data-note-display-switch="" data-ng-class="{ 'active': noteDisplaySwitchCtrl.displayFlat }">
                    <a href="" style="font-size: 28px;" data-ng-click="noteDisplaySwitchCtrl.switchDisplay()">
                        &#9837;
                    </a>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                {# Collect header links of the current view #}
                {% for header_link in header_links %}
                <li role="presentation" class="nav-link-{{ header_link.class|default('default') }}">
                    <a href="{{ header_link.url }}">
                        {% if header_link.icon is defined %}
                            <span class="fa fa-{{ header_link.icon }}"></span>
                        {% endif %}

                        {{ header_link.label|trans }}
                    </a>
                </li>
                {% endfor %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
