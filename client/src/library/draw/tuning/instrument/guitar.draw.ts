import {CanvasDraw} from "./../../canvas.draw";
import {Note} from "../../../../components/theory/note/note";

export class GuitarTunerDraw {
    public drawNote(note: Note, position: number): void {

    }
}